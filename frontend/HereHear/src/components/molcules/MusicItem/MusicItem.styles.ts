import styled from "styled-components";

const MusicItemWrapper = styled.div`
    display: flex;
`;

const MusicTextWrapper = styled.div`
    display: flex;
    flex-direction: column;
    justify-content: space-evenly;
    margin-left: 16px;
`;

export { MusicItemWrapper, MusicTextWrapper };
